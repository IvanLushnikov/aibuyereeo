# 🔧 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Respond to Webhook возвращает шаблон

## Проблема
n8n возвращает необработанный шаблон `{{ $json.reply }}` вместо реального значения.

**В логах видно:**
```
[N8NClient] Ответ от n8n: {"reply":"{{ $json.reply }}"}
```

## Причина
В ноде **"Respond to Webhook"** выражение `{{ $json.reply }}` не обрабатывается - n8n возвращает его как строку.

## Решение

### Вариант 1: Исправить в ноде "Respond to Webhook"

1. Откройте ноду **"Respond to Webhook"** в n8n
2. В поле **"Response Body"** должно быть:
   ```json
   {
     "reply": "{{ $json.reply }}"
   }
   ```
3. **Проблема:** n8n не обрабатывает `{{ $json.reply }}` - возвращает как строку

### Вариант 2: Использовать правильный путь к данным

Проблема в том, что данные из предыдущей ноды (Code in JavaScript) могут быть в другом формате.

**Проверьте:**
1. Откройте ноду **"Code in JavaScript"** (которая идет перед "Respond to Webhook")
2. Посмотрите, что она возвращает в `return`
3. Возможно, данные нужно брать из `$json.output.reply` или `$json[0].reply`

### Вариант 3: Исправить в "Code in JavaScript"

Если нода "Code in JavaScript" возвращает данные, убедитесь что она возвращает:
```javascript
return {
  reply: $json.reply || $json.output?.reply || "..." // ваш код
};
```

### Вариант 4: Использовать прямой доступ к данным

В "Respond to Webhook" попробуйте:
- `{{ $json.output.reply }}` - если данные в output
- `{{ $json[0].reply }}` - если это массив
- `{{ $json.reply }}` - если данные прямо в корне

## Как проверить

1. Откройте n8n → ваш workflow
2. Запустите тестовый execution
3. Откройте ноду **"Respond to Webhook"** → вкладка **"Output"**
4. Посмотрите, что там в **INPUT** - там должны быть данные с полем `reply`
5. Если в INPUT нет `reply` - проблема в предыдущей ноде

## Быстрое решение

1. Откройте ноду **"Code in JavaScript"** (перед "Respond to Webhook")
2. Убедитесь, что она возвращает объект с полем `reply`:
   ```javascript
   const reply = $json.reply || $json.output?.reply || "Ошибка: нет reply";
   
   return {
     reply: reply
   };
   ```
3. В ноде **"Respond to Webhook"** используйте:
   ```json
   {
     "reply": "{{ $json.reply }}"
   }
   ```
4. Проверьте, что в настройках "Respond to Webhook" включен режим обработки выражений

## Проверка в n8n

1. Запустите workflow
2. Откройте execution → ноду "Respond to Webhook"
3. Посмотрите **INPUT** - там должны быть данные с `reply`
4. Посмотрите **OUTPUT** - там должен быть обработанный JSON с реальным текстом, а не шаблоном

Если в OUTPUT все еще `{{ $json.reply }}` - значит n8n не обрабатывает выражения. Проверьте настройки ноды.

