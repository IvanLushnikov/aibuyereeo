# 🔧 Простое исправление ошибки n8n

## Ошибка
```
input values have 3 keys, you must specify an input key or pass only 1 key as input
```

## Решение

Откройте ноду **"AI Agent"** в n8n и:

1. Найдите поле **"Input Key"** или **"Input Data"**
2. Укажите **один ключ**, например: `message`
   
   ИЛИ

3. В настройках AI Agent найдите **"Input"** или **"Data Source"**
4. Выберите **"Use first item"** или **"Use JSON"**
5. В поле для ввода данных укажите: `{{ $json.message }}`

---

## Альтернативное решение (если первое не работает)

### Вариант 1: Использовать только message

В настройках AI Agent в поле **"Input"** или **"Message"** укажите:
```
{{ $json.message }}
```

### Вариант 2: Использовать Set Node перед AI Agent

Добавьте ноду **"Set"** между Webhook и AI Agent:

1. Добавьте ноду **"Set"**
2. В поле **"Values to Set"** добавьте:
   - **Name:** `input`
   - **Value:** `{{ $json.message }}`
3. Теперь в AI Agent используйте: `{{ $json.input }}`

---

## Что точно НЕ нужно менять

- ✅ **Code in JavaScript** - оставьте как есть (`$json.output` или `$json.reply`)
- ✅ **Respond to Webhook** - оставьте как есть (`{{ $json.reply }}`)
- ✅ **Логирование** - если работает, не трогайте

---

## Быстрый способ

1. Откройте ноду **AI Agent**
2. Найдите поле где вводится сообщение/промпт
3. Там должно быть что-то вроде `{{ $json.message }}` или просто `message`
4. Если там указано несколько ключей или весь `$json`, оставьте только `message`
5. Сохраните и протестируйте

