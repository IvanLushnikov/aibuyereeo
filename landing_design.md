## Дизайн-концепция лендинга «Робот Семён»

### 1. Визуальная идея
- **Mood:** «ночной город / неоновый хаб» — сочетание тёмного фона (#080B1A) с градиентами фуксия → бирюза → электрик.
- **Основные цвета:**
  - Gradient A: `#7D2FFF` → `#00E7FF`
  - Gradient B (CTA): `#FF5F8D` → `#FFCF4D`
  - Дополнительные акценты: `#1C223C`, `#2E4FFC`, `#79FFB7`
- **Шрифты:**
  - Заголовки — `Manrope` ExtraBold (или `Space Grotesk` для более tech-вайба)
  - Текст — `Inter`/`Onest` Regular
- **Элементы:** стеклянные карточки (glassmorphism), мягкие тени, эффект свечения у кнопок, 3D-иллюстрация/иконка робота (плоская, контурная, с неоновыми гранями).

### 2. Компонентный разбор
- **Hero:**
  - Фон: анимация градиента (CSS `background-position` + `@keyframes` 20 сек).
  - Заголовок с легким blur, highlight слова «Секунды».
  - Motion path для иконок (как будто поток данных).
- **Блок «Кому это нужно»:** три карточки, всплывающий текст при hover; иконки ролей.
- **Блок «Как работает»:** последовательность с линией и точками, light-mode подсветка активного шага.
- **Боль → Решение:** таблица в виде двух колонок, разделённых неоновой линией.
- **Цитаты:** speech bubble карточки с аватарками-иконками (инициатор, техспец, закупщик), на фоне — диагональная сетка.
- **FAQ:** аккордеон с microinteraction (иконка ↗ раскрывается, фон подсвечивается).
- **Форма:** стеклянная панель, градиентный border, success-state — всплывающая карточка с эмодзи 🤖.

### 3. Чат-виджет
- **Плавающая кнопка:** круглая, градиент B, icon robot, тень + эффект «дыхания» (scale 1 ↔ 1.05).
- **Окно чата:**
  - ширина 360px (desktop), full-width bottom sheet (mobile), стеклянное окно.
  - верхняя панель с аватаром Семёна и статусом «онлайн/думает».
  - сообщения user/agent: user — фиолетовые пузыри справа, агент — тёмные с неоновой рамкой слева.
  - индикатор typing: три неоновые точки.
- **Анимации:** `framer-motion` для появления/скрытия, `auto-scroll` вниз при новых сообщениях.
- **Состояния:**
  - `loading` — skeleton сообщений.
  - `error` — ред тряска + кнопка «попробовать снова».
  - `feedback` — после 3 сообщений предлагать быстрый опрос (полезно? да/нужен человек).

### 4. Адаптив
- **Desktop ≥ 1280px:** многоколонные секции, анимации параллакса.
- **Tablet 768–1279px:** карточки в 2 столбца, чат занимает 320px справа внизу.
- **Mobile ≤ 767px:** однострочная структура, упрощённые тени, чат разворачивается на весь экран.
- **Находки:** отключить сложные градиентные анимации на слабых устройствах (prefers-reduced-motion, медиазапрос).

### 5. Гайд по анимациям
- Восходящий fade-up при прокрутке (`framer-motion`): delay 0.1-0.3s.
- CTA при hover — лёгкий tilt + светящийся обвод.
- Hero background — плавная смена по диагонали (20s loop, ease-in-out).
- Чат кнопка — пульс каждые 4s, amplitude 5%.
- FAQ — иконка расширяется до стрелки, контент разворачивается за 200ms.

### 6. Контентные состояния
- **Dark mode only:** основной дизайн, но предусмотреть light fallback (white background, темные карточки) или CSS override.
- **Empty states:** если нет ответа от бота — карточка «Семён сейчас занят, попробуйте ещё раз» с иконкой песочных часов.
- **Loading placeholder:** вместо текста — shimmer эффект.

### 7. Иллюстрации и ассеты
- Иконка робота: создать SVG (голова, антенны, улыбка) с неоновой подсветкой.
- Использовать SVG-паттерны (диагональная сетка, микрочипы) в фоне блоков.
- Для блоков «Как работает» — маленькие иллюстрации (описание → чат → таблица → экспорт).

### 8. Checklist для дизайна
- [ ] Подготовить moodboard и цветовые комбинации
- [ ] Сверстать Hero и карточки ролей в Figma
- [ ] Прототип чата (desktop + mobile)
- [ ] Адаптивные макеты Tablet/Mobile
- [ ] Стиль иконок/иллюстраций

