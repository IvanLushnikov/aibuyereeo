{
  "name": "aibuyereeo-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for aibuyereeo landing app",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "cd landing-app && npm install && npm run build",
    "start": "cd landing-app && npm start"
  }
}

